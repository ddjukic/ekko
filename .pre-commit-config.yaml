repos:
  # Ruff for linting and formatting
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.6  # Use the latest version
    hooks:
      # Run the linter first to catch issues
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
        name: ruff (lint and fix)
      # Run the formatter
      - id: ruff-format
        name: ruff (format)

  # Check for trailing whitespace and file endings
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: [--maxkb=5000]
      - id: check-json
      - id: check-toml
      - id: check-merge-conflict
      - id: debug-statements  # Check for leftover debugger imports
      - id: mixed-line-ending
        args: [--fix=lf]

  # Check for secrets
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        args: ['--baseline', '.secrets.baseline']
        exclude: .*\.lock$|package-lock\.json$

  # Type checking with mypy (optional, can be slow)
  # Commented out by default since it can be slow
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.13.0
  #   hooks:
  #     - id: mypy
  #       additional_dependencies: [types-all]
  #       args: [--config-file=pyproject.toml]
